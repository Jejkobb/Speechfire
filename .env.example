# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# Example for localhost only (default for non-Docker)
# SERVER_HOST=127.0.0.1
# SERVER_PORT=5000

# Example for custom port
# SERVER_PORT=8080

# Whisper model configuration
# Available models: tiny, base, small, medium, large-v1, large-v2, large-v3
# Larger models are more accurate but slower and require more memory
WHISPER_MODEL=base

# Examples:
# WHISPER_MODEL=tiny     # Fastest, least accurate (~40MB)
# WHISPER_MODEL=base     # Balanced (default) (~150MB)
# WHISPER_MODEL=small    # Better accuracy (~500MB)
# WHISPER_MODEL=medium   # High accuracy (~1.5GB)
# WHISPER_MODEL=large-v3 # Best accuracy (~3GB)